version: '3.8'

services:
  pdf-ocr-api:
    build: .
    container_name: pdf-ocr-api
    ports:
      - "8000:8000"
    volumes:
      - ./main.py:/app/main.py
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
